<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    <ui:style type="com.movierating.client.ui.home.MoviePoster.MyStyle">
        .poster {
            display: inline-block;
            margin-right: 1.6rem;
            position: relative;
            min-width: 160px;
            min-height: 300px;

            max-height: 320px;
            max-width: 160px;

            overflow: hidden;
            vertical-align: top;
        }
        .img_wrapper {
            position: relative;
        }
        .title_wrapper{
            font-weight: bold;
            padding: 2px 4px 2px 6px;
            margin-top: 8px;
            white-space: normal;
            min-height: 40px;
            max-width: 130px;
            text-align: left;
            line-height: 1.1rem;
        }
        .moviescore_w {
            position: absolute;
            top: 6px;
            right: 6px;

            vertical-align: middle;
            text-align: center;
            color: #fff!important;
            font-family: Arial,Helvetica,sans-serif;
            font-style: normal!important;
            font-weight: bold!important;
            background-color: transparent;
            height: 2em;
            width: 2em;
            line-height: 2em;
            font-size: 17px;
            border-radius: 6px;
        }
        .moviescore_w.positive {
            background-color: #6c3;
        }
        .moviescore_w.negative {
            background-color: #f00;
        }

        .moviescore_w.mixed {
            background-color: #fc3;
        }

        .moviescore_w.large{
            font-size: 25px;
        }
    </ui:style>
    <g:HTMLPanel addStyleNames="{style.poster}">
        <div ui:field="imgDiv" class="{style.img_wrapper}">
            <g:Image ui:field="posterImage"></g:Image>
        </div>

        <div ui:field="titleDiv" class="{style.title_wrapper}">
            <g:Label ui:field="titleLabel"></g:Label>
        </div>

        <div ui:field="ratingDiv" class="{style.moviescore_w} {style.large}">
        </div>
<!--                <g:VerticalPanel ui:field="posterPanel">-->
<!--                </g:VerticalPanel>-->
    </g:HTMLPanel>
</ui:UiBinder>